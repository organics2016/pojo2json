package testdata.kotlin

class PrimitiveArrayTestPOJO {
    private val bytes = byteArrayOf(0)
    private val shorts = shortArrayOf(0)
    private val ints = intArrayOf(0)
    private val longs = longArrayOf(0L)
    private val floats = floatArrayOf(0.00f)
    private val doubles = doubleArrayOf(0.00)
    private val booleans = booleanArrayOf(false)
    private val chars = charArrayOf('c')
}